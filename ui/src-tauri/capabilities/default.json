{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default Quill capabilities — grants core and global-shortcut permissions to both windows. Clipboard I/O goes through the Rust `arboard` crate, so no clipboard-manager plugin is needed.",
  "windows": ["mini", "full"],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}
